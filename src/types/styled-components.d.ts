import 'styled-components';

declare module 'styled-components' {
  export interface DefaultTheme {
    color: {
      white: string;
      turquoise: string;
      grayLight: string;
      gray: string;
      grayDark: string;
      grayDarker: string;
      ruby: string;
    }
  }
}
